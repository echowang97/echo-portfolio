# 内容 TODO（明天填）

> 内容几乎都在 `js/content.js`。图放 `assets/`（封面类放 `assets/covers/`），改完刷新即生效。
> 标 `〔〕` 的是占位/待确认；`shot: null` / `items: []` 是缺图的位置。

## About（关于我）
- [ ] 个人照片：放 `assets/echo.jpg`，把 `ABOUT.photo` 从 `null` 改成路径（竖图最佳）
- [ ] 小红书二维码：`SOCIAL` 里 `xiaohongshu.qr` 填图路径
- [ ] 微信二维码：`SOCIAL` 里 `wechat.qr` 填图路径

## Delysium
- [ ] 官网描述确认：`delysium.com` 的一句话定位（现为草稿）
- [ ] dna.delysium.com 是什么（desc 占位）
- [ ] dashboard.delysium.com 用途（desc 占位）
- [ ] 「海报 / 物料」图堆是空的 → 往 `delysium` 的 `poster` pile 的 `items` 加图

## Kuse
- [ ] 「Kuse App」图堆只有 1 张 → 补更多 app 界面截图（`app` pile）
- [ ] 「海报」图堆是空的 → 补图（`poster` pile）
- [ ] 「物料」：确认 kuse.love(Kuse Shop) 的 URL；补更多物料图（`material` pile）
- [ ] `lucyos.png`（在 cover 文件夹里）还没归类 → 告诉我它是什么、放哪个项目哪个堆

## Junior
- [ ] 「设计系统 / 物料」图堆是空的 → 补组件库 / token 截图（`design` pile）

## Recycle Bin（回收站，过往 / 未上线）
- [ ] Delysium ONE：补一句说明（做到哪、为什么没上）
- [ ] sip：一句话说明 + 封面图（`shot`）
- [ ] NOFA：一句话说明 + 封面图（`shot`）
- [ ] 百度游戏：封面 / 代表作品图（`shot`）

## Resume
- [ ] 简历 PDF：放 `assets/`，把 `RESUME.pdf` 填上路径（填了会自动出现「下载 PDF」按钮）

## 其它
- [ ] 通读一遍各项目描述措辞，按需润色
- [ ] 决定 CRT 效果默认开还是关（现在默认开，`js/desktop.js` 的 `initCrtToggle` 里 `let on = true`）
